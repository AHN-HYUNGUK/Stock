name: Auto Telegram Bot

on:
  schedule:
    # 오전 6시 (UTC 기준 → 한국 시간 15시)
    - cron: '0 21 * * *'
    # 오후 2시 (UTC 기준 → 한국 시간 5시)
    - cron: '0 5 * * *'
  workflow_dispatch:  # 수동 실행도 가능

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run bot
        env:
         OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}  # ✅ 여기에 추가
          TOKEN: ${{ secrets.TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          NEWSAPI: ${{ secrets.NEWSAPI }}
          EXCHANGEAPI: ${{ secrets.EXCHANGEAPI }}
          TWELVEDATA_API: ${{ secrets.TWELVEDATA_API }}
        run: python main.py
